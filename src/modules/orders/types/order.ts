export enum ProductType {
  CANDLE = 'candle',
  PRAYER = 'prayer',
}

export enum CandleType {
  HEALTH = 'health',
  REST = 'rest',
}

export enum State {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
}
